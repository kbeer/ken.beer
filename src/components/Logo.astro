---
interface Props {
  size?: 'sm' | 'md' | 'lg';
}

const { size = 'md' } = Astro.props;

const sizes = {
  sm: { text: '1.2rem', dot: '0.3rem', gap: '0.15rem' },
  md: { text: '1.4rem', dot: '0.35rem', gap: '0.2rem' },
  lg: { text: '2.2rem', dot: '0.5rem', gap: '0.3rem' },
};

const s = sizes[size];
---

<span class="logo" style={`--text-size: ${s.text}; --dot-size: ${s.dot}; --gap: ${s.gap};`}>
  <span class="text">KEN</span><span class="dot"></span><span class="text">BEER</span>
</span>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Archivo:wght@600&display=swap');

  .logo {
    font-family: 'Archivo', system-ui, sans-serif;
    font-weight: 600;
    font-size: var(--text-size);
    letter-spacing: 0.1em;
    display: inline-flex;
    align-items: center;
    gap: var(--gap);
    color: var(--sl-color-text);
  }

  .text {
    text-transform: uppercase;
  }

  .dot {
    width: var(--dot-size);
    height: var(--dot-size);
    background: var(--sl-color-accent);
    border-radius: 50%;
    display: inline-block;
  }
</style>
